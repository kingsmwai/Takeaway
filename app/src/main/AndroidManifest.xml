<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.kingsmwai.pizza_order">

    <uses-permission android:name="android.permission.INTERNET" />

    <uses-permission-sdk-23 android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Pizza_order"
        tools:ignore="ExtraText"
        tools:replace="android:theme">
        <activity android:name=".Controller.IntroScreenTwo"></activity>
        <activity android:name=".Controller.IntroScreen" />
        <activity android:name=".Controller.SignInActivityy" />
        <activity android:name=".Controller.ProductDetailActivityy" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
        <activity android:name=".Controller.SignUpActivityy" />
        <activity android:name=".Controller.ProductListActivity" />
        <activity android:name=".Controller.HomeActivity" />
        <activity android:name=".Controller.CartActivity" />
        <activity android:name=".MainActivity">
        </activity>
    </application>

</manifest>